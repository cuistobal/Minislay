caca
minislay > exit
coucou
caca
minislay > exit
coucou
caca
minislay > exit
echo coucou && echo caca >> infile.txt || << EOF grep 'pommes' > outfile.txt
coucou && echo caca >> infile.txt || << EOF grep 'pommes' > outfile.txt
&& echo caca >> infile.txt || << EOF grep 'pommes' > outfile.txt
echo caca >> infile.txt || << EOF grep 'pommes' > outfile.txt
caca >> infile.txt || << EOF grep 'pommes' > outfile.txt
>> infile.txt || << EOF grep 'pommes' > outfile.txt
infile.txt || << EOF grep 'pommes' > outfile.txt
|| << EOF grep 'pommes' > outfile.txt
<< EOF grep 'pommes' > outfile.txt
EOF grep 'pommes' > outfile.txt
grep 'pommes' > outfile.txt
'pommes' > outfile.txt
> outfile.txt
outfile.txt
coucou
caca
minislay > exit
