#include "minislay.h"

Expansion found ? realloc

//Entry point of the expansion module
bool    expand(t_shel *minishell, char *token)
{
    int     index;
    char    expanded;

    if (minishell && token)
    {
        index = 0;
        while ()
    }
}
